# Input Matches

run $command
execute $command
run in terminal $command
run shell command $command

# Output

```c_template
execute-shell
```

```c_command
$command
```

# Note

c_template and c_command are constants
